import { ReportContentHostDirective } from './report-content-host.directive';

describe('ReportContentHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ReportContentHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
