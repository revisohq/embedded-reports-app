import { ReportHostDirective } from './report-host.directive';

describe('ReportHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ReportHostDirective(null);
    expect(directive).toBeTruthy();
  });
});
